local _ = require("gettext")

return {
    fullname = _("Book Room"),
    description = _([[Shows KOReader chapter data and can manually send the current chapter to Book Room using the existing KOSync connection.]]),
    version = "0.4.0",
}
