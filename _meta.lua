local _ = require("gettext")

return {
    fullname = _("Book Room"),
    description = _([[Observes KOReader chapter changes and sends the latest chapter to Book Room using the existing KOSync connection.]]),
    version = "0.6.0",
}
